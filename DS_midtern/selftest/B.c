// 目的：計算多個測試案例中每段區間的最大收益
// 輸入：M - 測試案例的數量
//       每個測試案例包括 N - 價格數量
//       價格數組 - 包含 N 個整數的數組，代表股票的價格
// 輸出：每個測試案例的最大收益

    // 函數用來計算每段區間的最大收益和最低價格
    // 用於存儲測試案例數量
    // 用於存儲每個測試案例中的價格數量
    // 從輸入中讀取測試案例數量
    // 開始處理每個測試案例
    // 用於存儲當前價格
    // 初始最低價格為正無限大
    // 初始最大收益為0
    // 從輸入中讀取價格數量
    // 開始處理每個價格
    // 從輸入中讀取當前價格
    // 調用函數計算最大收益和最低價格
    // 輸出最大收益


    // 初始化最大收益為0
    // 滑動窗口的起始位置
    // 初始化最低價格為第一個價格
    // 從第二個價格開始迭代
    // 更新最低價格
    // 如果找到更低的價格，更新最低價格
    // 計算當前價格賣出時的收益，並更新最大收益
    // 計算當前區間的收益
    // 如果當前收益大於最大收益，則更新最大收益
    // 返回最大收益
    // 輸入要處理的數量 M
    // 開始處理每個測試案例
    // 輸入價格數量 N
    // 創建一個用於存儲價格的數組
    // 輸入每個價格
    // 調用 max_profit 函數計算最大收益
    // 輸出最大收益結果


    // 從第二個價格開始迭代

    // 如果找到更低的價格，更新最低價格
    // 計算當前價格賣出時的收益，並更新最大收益
    // 計算當前區間的收益
    // 如果當前收益大於最大收益，則更新最大收益
    // 返回最大收益
    // 輸入要處理的數量 M
    // 開始處理每個測試案例
    // 輸入價格數量 N
    // 創建一個用於存儲價格的數組
    // 輸入每個價格
    // 調用 max_profit 函數計算最大收益
    // 輸出最大收益結果